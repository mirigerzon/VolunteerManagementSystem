<Window x:Class="PL.Call.CallsListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:bo="clr-namespace:BO;assembly=BL"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:converters="clr-namespace:PL.Converters"
        Title="Calls List"
        Height="650"
        Width="1200"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <ObjectDataProvider x:Key="CallSortFieldCollectionKey" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:OpenCallSortField"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="CallStatusCollectionKey" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:CallStatus"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="CallTypeCollectionKey" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:CallType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <converters:NullableBooleanConverter x:Key="NullableBooleanConverter"/>
        <converters:NullableEnumConverter x:Key="NullableEnumConverter"/>
        <converters:CanDeleteCallConverter x:Key="CanDeleteCallConverter"/>
        <converters:IsStatusAssignedConverter x:Key="IsStatusAssignedConverter"/>
        <converters:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter"/>

        <Style TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="DataGridActionButton" TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <Label Content="Sort by:" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource CallSortFieldCollectionKey}}"
                      SelectedValue="{Binding SelectedSortField}" Width="130"/>

            <Label Content="Active:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding IsActiveFilterOptions}" SelectedItem="{Binding SelectedIsActiveFilter}" Width="100">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource NullableBooleanConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Label Content="Status:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding CallStatusOptions}" SelectedItem="{Binding SelectedCallStatusFilter}" Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource NullableEnumConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Label Content="Type:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding CallTypeOptions}" SelectedItem="{Binding SelectedCallTypeFilter}" Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource NullableEnumConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding CallList}"
                  SelectedItem="{Binding SelectedCall}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  RowHeight="30">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding CallStatus}" Width="100">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="{Binding CallStatus, Converter={StaticResource StatusToBackgroundConverter}}"/>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Type" Binding="{Binding CallType}" Width="100"/>
                <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" Width="140"/>
                <DataGridTextColumn Header="Time Left" Binding="{Binding TimeLeft, StringFormat=\{0:mm\\:ss\}}" Width="100"/>
                <DataGridTextColumn Header="Last Volunteer" Binding="{Binding LastVolunteerName}" Width="150"/>
                <DataGridTextColumn Header="Treatment Time" Binding="{Binding TreatmentDuration}" Width="120"/>
                <DataGridTextColumn Header="Total Assignments" Binding="{Binding TotalAssignments}" Width="80"/>
                <DataGridTextColumn Header="Raw Status" Binding="{Binding Status}" Width="100"/>

                <DataGridTemplateColumn Header="Delete" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeleteCall_Click" Style="{StaticResource DataGridActionButton}">
                                <Button.Visibility>
                                    <MultiBinding Converter="{StaticResource CanDeleteCallConverter}">
                                        <Binding Path="CallStatus"/>
                                        <Binding Path="TotalAssignments"/>
                                    </MultiBinding>
                                </Button.Visibility>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Cancel" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Cancel" Click="CancelAssignment_Click" Style="{StaticResource DataGridActionButton}"
                                    CommandParameter="{Binding}"
                                    Visibility="{Binding CallStatus, Converter={StaticResource IsStatusAssignedConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add Call -->
        <Button Grid.Row="2" Content="Add New Call" Height="40" Width="180"
                HorizontalAlignment="Center" Margin="10" Click="AddCall_Click"/>
    </Grid>
</Window>
