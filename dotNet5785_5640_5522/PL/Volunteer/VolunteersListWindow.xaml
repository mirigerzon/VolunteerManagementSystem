<Window x:Class="PL.Volunteer.VolunteersListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:bo="clr-namespace:BO;assembly=BL"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:converters="clr-namespace:PL.Converters"
        Title="רשימת מתנדבים"
        Height="600" Width="1050"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="VolunteerSortFieldCollectionKey">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:VolunteerSortField"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <converters:NullableBooleanConverter x:Key="NullableBooleanConverter"/>
        <Style TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <StackPanel Orientation="Horizontal" Margin="10" DockPanel.Dock="Top">
            <Label Content="מיין לפי:" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource VolunteerSortFieldCollectionKey}}"
                      SelectedValue="{Binding SelectedSortField}" Width="150" Margin="5,0"/>

            <Label Content="סנן לפי פעיל:" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <ComboBox ItemsSource="{Binding IsActiveFilterOptions}" Width="100"
                      SelectedItem="{Binding SelectedIsActiveFilter}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource NullableBooleanConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <DataGrid ItemsSource="{Binding VolunteerList}"
                  SelectedItem="{Binding SelectedVolunteer}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ת.ז" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="שם מלא" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="קריאות שטופלו" Binding="{Binding TotalHandledCalls}"/>
                <DataGridTextColumn Header="קריאות שבוטלו" Binding="{Binding TotalCanceledCalls}"/>
                <DataGridTextColumn Header="פג תוקף" Binding="{Binding ExpiredCallsCount}"/>
                <DataGridTextColumn Header="קריאה נוכחית" Binding="{Binding CurrentCallId}"/>
                <DataGridTextColumn Header="סוג קריאה" Binding="{Binding CallType}"/>
                <DataGridCheckBoxColumn Header="פעיל" Binding="{Binding IsActive}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="הוסף מתנדב" Width="150" Height="40" Margin="10"
                HorizontalAlignment="Center" Click="AddVolunteer_Click" DockPanel.Dock="Bottom"/>
    </DockPanel>
</Window>
