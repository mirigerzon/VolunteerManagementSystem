<Window x:Class="PL.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="התחברות למערכת" Height="320" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Border Background="#f7f7f7" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="התחברות למערכת" FontSize="20" FontWeight="Bold"
                       Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Label Content="תעודת זהות:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox x:Name="IdTextBox" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,0,10"
                     PreviewTextInput="IdTextBox_PreviewTextInput"/>

            <Label Content="סיסמה:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Height="30" Margin="0,0,0,10"/>

            <Button Content="התחבר" Grid.Row="3" Grid.ColumnSpan="2" Height="35" Margin="0,10,0,0"
                    Background="#007ACC" Foreground="White" FontWeight="Bold" Click="LoginButton_Click"/>

            <TextBlock x:Name="StatusTextBlock" Grid.Row="4" Grid.ColumnSpan="2" Foreground="Red"
                       TextWrapping="Wrap" Margin="0,15,0,0" TextAlignment="Center"/>
        </Grid>
    </Border>
</Window>
